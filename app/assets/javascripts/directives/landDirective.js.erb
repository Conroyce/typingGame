app.directive("landDirective", ["$http", "$window", function($http, $window) {
  return {
    templateUrl: "<%= asset_path('templates/directives/land_template.html') %>",
    link: function(scope, ele, attr) {

    }
  }
}]);